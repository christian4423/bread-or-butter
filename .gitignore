# App Store Connect API keys / secrets — never commit these
*.p8
docs/*.p8
docs/issue-key-id.json
docs/issuer-key-id.json

# Local upload helpers (throwaway)
xcode-task*.sh

# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
